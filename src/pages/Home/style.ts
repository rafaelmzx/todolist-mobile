import { StyleSheet } from "react-native"

export const homeStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
    },
    filterContainer: {
      padding: 16,
    },
    inputContainer: {
      flexDirection: 'row',
      padding: 16,
    },
    input: {
      flex: 1,
      marginRight: 16,
      borderWidth: 1,
      borderColor: '#ccc',
      padding: 8,
    },
})